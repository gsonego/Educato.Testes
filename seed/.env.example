# Conexão com o banco de dados de testes (NUNCA use credenciais de produção aqui)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=educato

# URL base da API de testes (ex.: http://localhost:8080). Opcional: sem ela os scripts
# `seed:tarjetas` e `seed:notas` são pulados, deixando as tabelas de tarjetas/notas vazias.
API_BASE_URL=
